// Virtual module produced by the esbuild build (see esbuild.config.mjs).
// The browser-side player bundle is inlined at build time as a string.
declare module "player-screen-bundle" {
  const playerJs: string;
  export default playerJs;
}
